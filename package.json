{
  "name": "sba_4_task_management_app",
  "version": "1.0.0",
  "description": "Create input fields for the task name, category, deadline, and an initial status (e.g., “In Progress”).\r Include an “Add Task” button that will add the task to the task list.\r Each task should be stored as an object with properties such as task name, category, deadline, and status.\r Add the task object to an array that holds all tasks.",
  "main": "plan.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
